diff --git a/dist/index.js b/dist/index.js
index 09b3d37bae05bfa798cef1e41d42a9f0e22f7e93..9c4c95baf03599c7118793c074a80c9e69d4825c 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -336,6 +336,7 @@ var openaiChatResponseSchema = (0, import_provider_utils3.lazySchema)(
           message: import_v42.z.object({
             role: import_v42.z.literal("assistant").nullish(),
             content: import_v42.z.string().nullish(),
+            reasoning_content: import_v42.z.string().nullish(),
             tool_calls: import_v42.z.array(
               import_v42.z.object({
                 id: import_v42.z.string().nullish(),
@@ -404,6 +405,7 @@ var openaiChatChunkSchema = (0, import_provider_utils3.lazySchema)(
             delta: import_v42.z.object({
               role: import_v42.z.enum(["assistant"]).nullish(),
               content: import_v42.z.string().nullish(),
+              reasoning_content: import_v42.z.string().nullish(),
               tool_calls: import_v42.z.array(
                 import_v42.z.object({
                   index: import_v42.z.number(),
@@ -866,12 +868,23 @@ var OpenAIChatLanguageModel = class {
     if (text != null && text.length > 0) {
       content.push({ type: "text", text });
     }
+    const reasoning = choice.message.reasoning_content;
+    if (reasoning != null && reasoning.length > 0) {
+      content.push({
+        type: 'reasoning',
+        text: reasoning
+      });
+    }
     for (const toolCall of (_a = choice.message.tool_calls) != null ? _a : []) {
+      const toolFn = toolCall.function;
+      if (toolFn == null || !toolFn.name) {
+        continue;
+      }
       content.push({
         type: "tool-call",
         toolCallId: (_b = toolCall.id) != null ? _b : (0, import_provider_utils5.generateId)(),
-        toolName: toolCall.function.name,
-        input: toolCall.function.arguments
+        toolName: toolFn.name,
+        input: toolFn.arguments
       });
     }
     for (const annotation of (_c = choice.message.annotations) != null ? _c : []) {
@@ -943,6 +956,7 @@ var OpenAIChatLanguageModel = class {
     let usage = void 0;
     let metadataExtracted = false;
     let isActiveText = false;
+    let isActiveReasoning = false;
     const providerMetadata = { openai: {} };
     return {
       stream: response.pipeThrough(
@@ -999,6 +1013,21 @@ var OpenAIChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            const reasoningContent = delta.reasoning_content;
+            if (reasoningContent) {
+              if (!isActiveReasoning) {
+                controller.enqueue({
+                  type: 'reasoning-start',
+                  id: 'reasoning-0',
+                });
+                isActiveReasoning = true;
+              }
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: 'reasoning-0',
+                delta: reasoningContent,
+              });
+            }
             if (delta.content != null) {
               if (!isActiveText) {
                 controller.enqueue({ type: "text-start", id: "0" });
@@ -1032,11 +1061,13 @@ var OpenAIChatLanguageModel = class {
                       message: `Expected 'function.name' to be a string.`
                     });
                   }
-                  controller.enqueue({
-                    type: "tool-input-start",
-                    id: toolCallDelta.id,
-                    toolName: toolCallDelta.function.name
-                  });
+                  if (toolCallDelta.function.name !== "") {
+                    controller.enqueue({
+                      type: "tool-input-start",
+                      id: toolCallDelta.id,
+                      toolName: toolCallDelta.function.name
+                    });
+                  }
                   toolCalls[index] = {
                     id: toolCallDelta.id,
                     type: "function",
@@ -1047,7 +1078,7 @@ var OpenAIChatLanguageModel = class {
                     hasFinished: false
                   };
                   const toolCall2 = toolCalls[index];
-                  if (((_h = toolCall2.function) == null ? void 0 : _h.name) != null && ((_i = toolCall2.function) == null ? void 0 : _i.arguments) != null) {
+                  if (((_h = toolCall2.function) == null ? void 0 : _h.name) != null && _h.name !== "" && ((_i = toolCall2.function) == null ? void 0 : _i.arguments) != null) {
                     if (toolCall2.function.arguments.length > 0) {
                       controller.enqueue({
                         type: "tool-input-delta",
@@ -1075,6 +1106,17 @@ var OpenAIChatLanguageModel = class {
                 if (toolCall.hasFinished) {
                   continue;
                 }
+                if (toolCallDelta.function != null && toolCallDelta.function.name != null && toolCallDelta.function.name !== "") {
+                  const hadToolName = toolCall.function.name !== "";
+                  toolCall.function.name = toolCallDelta.function.name;
+                  if (!hadToolName) {
+                    controller.enqueue({
+                      type: "tool-input-start",
+                      id: toolCall.id,
+                      toolName: toolCallDelta.function.name
+                    });
+                  }
+                }
                 if (((_k = toolCallDelta.function) == null ? void 0 : _k.arguments) != null) {
                   toolCall.function.arguments += (_m = (_l = toolCallDelta.function) == null ? void 0 : _l.arguments) != null ? _m : "";
                 }
@@ -1083,7 +1125,7 @@ var OpenAIChatLanguageModel = class {
                   id: toolCall.id,
                   delta: (_n = toolCallDelta.function.arguments) != null ? _n : ""
                 });
-                if (((_o = toolCall.function) == null ? void 0 : _o.name) != null && ((_p = toolCall.function) == null ? void 0 : _p.arguments) != null && (0, import_provider_utils5.isParsableJson)(toolCall.function.arguments)) {
+                if (((_o = toolCall.function) == null ? void 0 : _o.name) != null && _o.name !== "" && ((_p = toolCall.function) == null ? void 0 : _p.arguments) != null && (0, import_provider_utils5.isParsableJson)(toolCall.function.arguments)) {
                   controller.enqueue({
                     type: "tool-input-end",
                     id: toolCall.id
@@ -1111,6 +1153,9 @@ var OpenAIChatLanguageModel = class {
             }
           },
           flush(controller) {
+            if (isActiveReasoning) {
+              controller.enqueue({ type: 'reasoning-end', id: 'reasoning-0' });
+            }
             if (isActiveText) {
               controller.enqueue({ type: "text-end", id: "0" });
             }
diff --git a/dist/index.mjs b/dist/index.mjs
index c408f82548073bd439060cff2b6f717a860b2255..1e13bbce67c795e840b4e9de87eee15cc3ad4b32 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -325,6 +325,7 @@ var openaiChatResponseSchema = lazySchema(
           message: z2.object({
             role: z2.literal("assistant").nullish(),
             content: z2.string().nullish(),
+            reasoning_content: z2.string().nullish(),
             tool_calls: z2.array(
               z2.object({
                 id: z2.string().nullish(),
@@ -393,6 +394,7 @@ var openaiChatChunkSchema = lazySchema(
             delta: z2.object({
               role: z2.enum(["assistant"]).nullish(),
               content: z2.string().nullish(),
+              reasoning_content: z2.string().nullish(),
               tool_calls: z2.array(
                 z2.object({
                   index: z2.number(),
@@ -857,12 +859,23 @@ var OpenAIChatLanguageModel = class {
     if (text != null && text.length > 0) {
       content.push({ type: "text", text });
     }
+    const reasoning = choice.message.reasoning_content;
+    if (reasoning != null && reasoning.length > 0) {
+      content.push({
+        type: 'reasoning',
+        text: reasoning
+      });
+    }
     for (const toolCall of (_a = choice.message.tool_calls) != null ? _a : []) {
+      const toolFn = toolCall.function;
+      if (toolFn == null || !toolFn.name) {
+        continue;
+      }
       content.push({
         type: "tool-call",
         toolCallId: (_b = toolCall.id) != null ? _b : generateId(),
-        toolName: toolCall.function.name,
-        input: toolCall.function.arguments
+        toolName: toolFn.name,
+        input: toolFn.arguments
       });
     }
     for (const annotation of (_c = choice.message.annotations) != null ? _c : []) {
@@ -934,6 +947,7 @@ var OpenAIChatLanguageModel = class {
     let usage = void 0;
     let metadataExtracted = false;
     let isActiveText = false;
+    let isActiveReasoning = false;
     const providerMetadata = { openai: {} };
     return {
       stream: response.pipeThrough(
@@ -990,6 +1004,21 @@ var OpenAIChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            const reasoningContent = delta.reasoning_content;
+            if (reasoningContent) {
+              if (!isActiveReasoning) {
+                controller.enqueue({
+                  type: 'reasoning-start',
+                  id: 'reasoning-0',
+                });
+                isActiveReasoning = true;
+              }
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: 'reasoning-0',
+                delta: reasoningContent,
+              });
+            }
             if (delta.content != null) {
               if (!isActiveText) {
                 controller.enqueue({ type: "text-start", id: "0" });
@@ -1023,11 +1052,13 @@ var OpenAIChatLanguageModel = class {
                       message: `Expected 'function.name' to be a string.`
                     });
                   }
-                  controller.enqueue({
-                    type: "tool-input-start",
-                    id: toolCallDelta.id,
-                    toolName: toolCallDelta.function.name
-                  });
+                  if (toolCallDelta.function.name !== "") {
+                    controller.enqueue({
+                      type: "tool-input-start",
+                      id: toolCallDelta.id,
+                      toolName: toolCallDelta.function.name
+                    });
+                  }
                   toolCalls[index] = {
                     id: toolCallDelta.id,
                     type: "function",
@@ -1038,7 +1069,7 @@ var OpenAIChatLanguageModel = class {
                     hasFinished: false
                   };
                   const toolCall2 = toolCalls[index];
-                  if (((_h = toolCall2.function) == null ? void 0 : _h.name) != null && ((_i = toolCall2.function) == null ? void 0 : _i.arguments) != null) {
+                  if (((_h = toolCall2.function) == null ? void 0 : _h.name) != null && _h.name !== "" && ((_i = toolCall2.function) == null ? void 0 : _i.arguments) != null) {
                     if (toolCall2.function.arguments.length > 0) {
                       controller.enqueue({
                         type: "tool-input-delta",
@@ -1066,6 +1097,17 @@ var OpenAIChatLanguageModel = class {
                 if (toolCall.hasFinished) {
                   continue;
                 }
+                if (toolCallDelta.function != null && toolCallDelta.function.name != null && toolCallDelta.function.name !== "") {
+                  const hadToolName = toolCall.function.name !== "";
+                  toolCall.function.name = toolCallDelta.function.name;
+                  if (!hadToolName) {
+                    controller.enqueue({
+                      type: "tool-input-start",
+                      id: toolCall.id,
+                      toolName: toolCallDelta.function.name
+                    });
+                  }
+                }
                 if (((_k = toolCallDelta.function) == null ? void 0 : _k.arguments) != null) {
                   toolCall.function.arguments += (_m = (_l = toolCallDelta.function) == null ? void 0 : _l.arguments) != null ? _m : "";
                 }
@@ -1074,7 +1116,7 @@ var OpenAIChatLanguageModel = class {
                   id: toolCall.id,
                   delta: (_n = toolCallDelta.function.arguments) != null ? _n : ""
                 });
-                if (((_o = toolCall.function) == null ? void 0 : _o.name) != null && ((_p = toolCall.function) == null ? void 0 : _p.arguments) != null && isParsableJson(toolCall.function.arguments)) {
+                if (((_o = toolCall.function) == null ? void 0 : _o.name) != null && _o.name !== "" && ((_p = toolCall.function) == null ? void 0 : _p.arguments) != null && isParsableJson(toolCall.function.arguments)) {
                   controller.enqueue({
                     type: "tool-input-end",
                     id: toolCall.id
@@ -1102,6 +1144,9 @@ var OpenAIChatLanguageModel = class {
             }
           },
           flush(controller) {
+            if (isActiveReasoning) {
+              controller.enqueue({ type: 'reasoning-end', id: 'reasoning-0' });
+            }
             if (isActiveText) {
               controller.enqueue({ type: "text-end", id: "0" });
             }
