1.没有实现ctrl -缩小页面，目前只能ctrl +放大

2.日记页面点击筛选后变成了全屏灰色遮罩，没有办法正确点击需要筛选的属性，修改后去检查一下是否顺利的按照我们的筛选条件搜索日记

3.写日记保存之后，返回到日记页面，应该保持筛选、月份选择状态

4.伙伴管理页面，点击新增伙伴的时候，切换页面应该有动画，和切换选项页面的动画一样

5.伙伴编辑页面，点击头像选择框之后已经有了图片选择器，所以我们不需要在头像下面显示提示“长按选择图片”这种。

6.伙伴编辑选择图片头像后，应该像用户更换头像一样显示编辑器

7.伙伴编辑页面选择默认模型之后，模型显示应该是模型供应商的icon+模型名称，就像模型选择器展示的效果，现在的效果是✨，需要修改

8.伙伴默认模型下面的提示“绑定后，使用此伙伴的会话将始终使用指定模型”，改成“绑定后，和伙伴创建对话时，会默认优先使用选择的模型”，注意处理好i18n支持

9.现在在网络搜索设置页面，选择搜索引擎的时候，当软件页面高度不够会无法显示全部选项，包括常规设置的卡片也无法显示完全，我需要无论何时必须全部显示，然后把提示里面的“可以绕过反爬虫机制”删除，并且规范用语，统一把Api密钥换成ApiKey，注意同步i18n修改

10.rag记忆管理页面的灰色没有全部铺满整个页面，现在滚动后会发现底色有灰白拼接

11.rag记忆管理页面的记忆显示应该做分页显示，默认10条一页，可以选择20、30、50、100

12.rag记忆管理页面关闭rag记忆的时候，提示语改一下，改成“RAG记忆功能已经关闭了喵~”，同步i18n

13.rag记忆管理页面的清空当前维度记忆按钮删除掉，因为我们已经有一个清空所有向量数据按钮了，把那个按钮的文字改成“清空现有记忆”

14.rag记忆管理页面，搜索记忆更改一下，在搜索框最右边加个状态按钮，用户可以切换开启语义搜索，如果没有开启就是纯文本搜索，开启就是文本+rag搜索，然后记得默认开启

15.检查我们现在的rag功能在切换模型的时候有没有顺利的完成备份记忆，然后重新嵌入新的维度记忆，测试打断后是否能够保持状态继续恢复

16.rag记忆管理页面，用户点击手动添加回忆按钮，确认添加的时候直接发送请求到向量嵌入模型，需要确保成功得到响应后才真的保存记忆，如果失败用我们规范的方式报错，记得同步i18n

17.回忆生成页面，两个card section相连接的地方不要有圆角，你直接改到同一个card section，然后周记，月报，季报，年鉴前面的emoji分别改成🌱☘️🪴🌳

18.我们现在需要一个文件支持系统，需要在日记里面能够上传添加附件，支持图片，视频，音频等，我需要的效果是类似于obsidian的感觉，用户复制粘贴进来，或者上传文件进来的时候，我们需要专门移动到一个附件文件夹进行管理，日记内的附件都应该配置在当月的文件夹里面，比如2026年5月7日的日记，在这里面上传的附件，应该存放到日记所在月份文件夹的attachment文件夹中规范存放。并且我需要能够让用户右键附件打开所在文件夹，还需要用户可以直接右键复制附件。这个地方我们要做的比较多了，还需要做图片的预览，用户还可以简单的放大缩小图片在日记里面显示的效果，所有附件都是引用链接，然后直接在我们的日记编辑器渲染，在主页面预览日记时也应该渲染媒体，这里也要顺便检查一下我们的日记预览渲染是否做了正确的部分渲染而不是全部渲染，这里我需要你帮我拆分成若干个任务，启用若干个sub agent进行工作。

19.在记忆画廊页面，用户点击侧边栏切换回忆的时候不进入编辑，仅仅是切换，用户点击编辑图标（笔）的时候，直接进入编辑页面，编辑页面和我们的日记编辑是一样的

20.日记页面在预览的时候，不管是选择了月份，还是选择了年份，都应该默认有分页，如果当前日记少于50条，就不显示分页组件，如果大于50条，就直接显示分页组件，分页组件应该是50、80、100，200

21.在常规设置-身份卡设置的地方，“descriptionText”和“chipsScrollArea”应该做一下内边距，也就是和软件边缘稍微保持一点的距离，不要紧挨着

22.伙伴聊天页面，打开计费面板，刷新价格表，如果网络出现问题控制台会提示
“@baishou/desktop:dev: [WARN] [ModelPricingService] prices fetch failed: Error: net::ERR_NAME_NOT_RESOLVED
@baishou/desktop:dev: at SimpleURLLoaderWrapper.<anonymous> (node:electron/js2c/browser_init:2:134240)
@baishou/desktop:dev: at SimpleURLLoaderWrapper.emit (node:events:508:28)”
但是没有正确的给用户反馈消息。

23.伙伴聊天页面，选择置顶伙伴的头像时，伙伴头像不需要缩小，直接在完全微微亮起表示选中就可以了

24.伙伴聊天页面，选中指定伙伴后，点击新会话，没有正确的在选中的伙伴下发起新会话，而是变回了原来的伙伴发起会话，这里要修复


26.我们需要在伙伴页面的输入框上面的工具栏里面加入一个tts选项，tts为“始终开启”，“手动触发”

27.我们需要在每一条ai伙伴回复的消息底部加上一个小喇叭的标识，点击之后调用tts模型，播放语音，如果用户在输入框上面的工具栏，将tts选择为始终开始，则每次ai回复之后自动调用tts模型，播放语音

28.检查我们现在的伙伴编辑页面，上下文轮数大小的选项是否生效，轮数应该是 用户一条消息，ai伙伴一次回复，作为一个完整的周期，并且要确保ai伙伴回复过程中涉及的所有工具调用，网络搜索等内容都隶属于对应的回复里，完整的截取

29.在伙伴编辑页面的记忆设置部分，状态和状态开关按钮在同一行，下面是一个名为“触发压缩条件”，滑条对应的token量显示和触发压缩条件在同一行，在触发压缩条件和保留记忆轮数右边加一个小按钮，用户把鼠标放上去会显示一个消息悬浮窗。
对于“触发压缩条件”，应该显示：“你可以设置和伙伴对话的上下文达到多少时触发上下文压缩”；对于“保留记忆轮数”，应该显示“在触发压缩后，你可以将压缩后的内容发送给伙伴，同时保留最近几轮会话的原文”

30.我们在伙伴页面的每次ai回复消息下，应该加入一个按钮，点击之后可以看到当前发送给ai消息的时候，我们具体发送了哪些上下文，这个做一个弹窗，这样可以允许用户看到发送的压缩内容有什么，原文内容有什么，提示词有什么，都要看到

31.我们需要一个对话分支功能，我们需要确保每条ai回复我们都可以从对应的位置创建分支，直接复制一个新会话出来，截取到选择的位置

32.现在在快捷指令面板创建快捷指令之后，指令面板显示指令会在前面显示一长串数字，可能是id？这个我们不需要。

33.快捷指令面板显示指令应该分页，默认5条一页，可以选择10，15，20，25，30
