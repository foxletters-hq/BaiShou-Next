export const STORY_INIT_SKILL_NAME = 'story-init'
export const WRITER_SKILL_NAME = STORY_INIT_SKILL_NAME
/** 旧官方技能目录名，启动时删除，不再保留 */
export const LEGACY_WRITER_SKILL_NAME = 'writer'

export const WRITER_SKILL_DESCRIPTION =
  '初始化长篇写作目录，并在每个目录写入规范。之后在该工作区写作时，先读对应目录的规范；改目录时同步改规范。'

export const WRITER_SKILL_CONTENT = `进入工作区后，先检查根目录，再决定是否初始化「story-init」写作模板。未经用户明确同意，不要创建、移动或覆盖任何文件。

本模板不只搭空文件夹。每个主要目录都要有 \`规范.md\`，写作根目录还要有 \`目录结构.md\`。之后写设定、大纲或正文时，必须先打开目标目录里的规范再动手。增删、重命名、移动文件夹时，必须在同一轮改 \`目录结构.md\` 和受影响目录的 \`规范.md\`。写作根目录是：空目录初始化时的工作区根；或非空目录下用户确认的新文件夹。

## 1. 先看目录

列出工作区根目录。以下名称视为噪音，不计入「已有内容」：\`.git\`、\`.gitignore\`、\`.DS_Store\`、\`Thumbs.db\`、\`desktop.ini\`。

- 排除噪音后没有任何文件或文件夹：视为空目录。
- 否则视为非空目录。

## 2. 询问，等回复

凡需要用户做选择或确认，必须调用 \`companion_ask\`，等工具返回后再继续。不要把问题写在普通回复里让用户打字回答。一次只提一件事。

\`companion_ask\` 参数：

- \`question\`：一句完整的问题
- \`options\`：可点选的选项，至少两个
- \`allow_custom_input\`：需要用户自拟名称时为 true，否则为 false

空目录：调用 \`companion_ask\`。\`question\` 写「是否在当前工作区根目录初始化写作目录和各目录规范？」；\`options\` 为「初始化」「先不创建」；\`allow_custom_input\` 为 false。用户选择「初始化」后再在工作区根目录创建。

非空目录：可先用一句话说明根目录已有内容，然后立即调用 \`companion_ask\`。\`question\` 写「要不要新建一个文件夹来存放整套小说架构？」；\`options\` 为「新建文件夹」「先不创建」；\`allow_custom_input\` 为 false。不要把模板文件直接铺进已有文件旁边。

- 用户选择「新建文件夹」：再调用一次 \`companion_ask\` 确认名称。\`question\` 写「文件夹用什么名字？」；\`options\` 只放一个当前未占用的建议名（优先 \`写作\`；若已占用则用 \`写作-2\`、\`写作-3\`）；\`allow_custom_input\` 为 true。用户自拟名称时，不能与现有文件或文件夹重名，也不能叫 \`设定\`、\`素材\`、\`大纲\`、\`小说\`、\`修订\`（这些是模板内部目录名）。名称确定后，把第 3 节的全部路径创建在该文件夹内。
- 用户选择「先不创建」，或拒绝作答：只说明现状并停止。不要把模板文件铺进当前根目录。
- 若用户主动要求就在当前根目录补齐：只补缺失路径；已存在的同名文件跳过，禁止覆盖。不要把「在当前目录补齐」做成默认选项。

得到 \`companion_ask\` 的明确答复后再动手。未经用户明确同意，不要创建、移动或覆盖任何文件。

## 3. 要创建的路径

在写作根目录创建（已存在则跳过）。路径都相对于写作根目录；若写作根是 \`写作/\`，则实际路径为 \`写作/目录结构.md\`、\`写作/设定/规范.md\`。

\`\`\`
目录结构.md
README.md
设定/
  规范.md
  世界观.md
  人物/
    规范.md
    _人物卡模板.md
  时间线.md
  术语表.md
素材/
  规范.md
  灵感.md
  参考资料/
  摘录.md
  图像/
大纲/
  规范.md
  总大纲.md
  章纲/
    规范.md
小说/
  规范.md
  卷一/
    规范.md
    001-030/
      规范.md
修订/
  规范.md
  问题清单.md
\`\`\`

不要提前创建 \`031-060\` 或卷二。写到第 31 章或开新卷时再新建，并立刻改规范。

使用工作区的目录创建与文件写入能力；不要用覆盖已有文件的方式写入。规范文件和空白稿纸里不要编造故事情节、人物姓名或世界观事实。

## 4. 各文件必须写入的正文

下面每段都是对应文件的完整正文。按路径写入；已存在则跳过。

### 目录结构.md

写入以下正文（目录树用缩进，不要再套代码围栏）：

# 目录结构（总规范）

在本工作区调整任何文件夹或约定前，先读本文件。改了目录，必须在同一轮改本文件，并改受影响目录里的 规范.md。

之后无论是补设定、改大纲还是写正文：先对照本文件确认路径，再进入目标目录读该目录的 规范.md。不要只凭记忆建新文件夹。

## 树

目录结构.md
README.md
设定/                  世界观、人物、时间线、术语；动笔前写这里
  规范.md
  世界观.md
  人物/
    规范.md
    _人物卡模板.md
  时间线.md
  术语表.md
素材/                  灵感、参考、摘录、图像；不是正文
  规范.md
  灵感.md
  参考资料/
  摘录.md
  图像/
大纲/                  全书骨架与章纲；与正文分开
  规范.md
  总大纲.md
  章纲/
    规范.md
小说/                  按卷、按每 30 章一夹存放稿件
  规范.md
  卷一/
    规范.md
    001-030/
      规范.md
修订/                  问题与审读；不改原稿所在位置
  规范.md
  问题清单.md

## 写作时怎么找规范

1. 读本文件，确认目标路径仍在树上。
2. 打开目标目录的 规范.md（子目录有自己的规范时，以该子目录为准，并遵守上级未取消的约定）。
3. 规范与实际目录不一致时，先改规范或先停下来问用户，不要悄悄改结构。

## 改目录时必须同时改规范

- 新增文件夹：写进上面的树，在该文件夹创建 规范.md。
- 重命名或移动：改树，并改所有提到旧路径的规范。
- 删除：从树中去掉，并写明内容去向（若已归档）。
- 小说分夹：第 31、61、91 章必须新建 031-060、061-090，并更新 小说/规范.md、当前卷 规范.md 和本文件。

（目录结构.md 正文到此结束）

### README.md

用几句话说明：这是长篇写作工作区；总约定在 \`目录结构.md\`；每个主要文件夹都有 \`规范.md\`。建议顺序：先补设定与大纲，再写 \`小说/卷一/001-030\`；素材随时补充；修订只记在 \`修订/\`。不要在这里写故事。

### 设定/规范.md

\`\`\`
# 设定规范

在本目录或子目录中创建、修改文件前，先读本文件。增删本目录下的文件夹时，同步改本文件和根目录 \`目录结构.md\`。

## 放什么

- 世界观、力量或规则、地理、势力：\`世界观.md\`
- 人物卡：\`人物/\`，新建人物复制 \`人物/_人物卡模板.md\`
- 事件先后：\`时间线.md\`
- 专有名词、译名、称呼：\`术语表.md\`

## 不放什么

故事正文、章纲、灵感摘录。它们分别在 \`小说/\`、\`大纲/\`、\`素材/\`。

## 写法

只写已确定的设定。未定的标「待定」，不要为了填满而编造。改设定时在原文处改，并检查 \`大纲/\` 与已写章节是否需要在 \`修订/问题清单.md\` 记一笔。
\`\`\`

### 设定/世界观.md

只写标题和空白小节：世界概要、规则、地理、势力。不要编内容。

### 设定/人物/规范.md

\`\`\`
# 人物目录规范

每人一张卡，复制 \`_人物卡模板.md\` 后改名，例如 \`林秋.md\`。不要在本目录写章节正文。新增或重命名人物文件不必改根目录树，但若新增子文件夹，必须改本文件和 \`目录结构.md\`。
\`\`\`

### 设定/人物/_人物卡模板.md

字段：姓名、身份、外表、动机、与主线关系、已知秘密、备注。各字段留空。

### 设定/时间线.md

标题加「按时间先后一条一行；未发生的标待定」。不要编事件。

### 设定/术语表.md

标题加「名词 / 定义 / 出现章节」。不要编词条。

### 素材/规范.md

\`\`\`
# 素材规范

本目录只收灵感、参考、摘录和图像，不直接当正文粘进 \`小说/\`。引用进正文前，先核对 \`设定/\` 是否已有对应条目。增删子文件夹时改本文件和 \`目录结构.md\`。
\`\`\`

### 素材/灵感.md、素材/摘录.md

各写一行用途说明，不要编条目。

### 大纲/规范.md

\`\`\`
# 大纲规范

\`总大纲.md\` 写全书骨架（主题、主线、卷划分）。单章或弧线的细纲放 \`章纲/\`，不要写进 \`小说/\`。改大纲后，若已写章节受影响，在 \`修订/问题清单.md\` 记下。增删子文件夹时改本文件和 \`目录结构.md\`。
\`\`\`

### 大纲/总大纲.md

空白小节：主题、主角与核心冲突、卷划分、结局意向（可待定）。不要编情节。

### 大纲/章纲/规范.md

\`\`\`
# 章纲规范

一章或一个弧线一份细纲。文件名与小说章号对齐，例如 \`第001章.md\`。这里只写节拍和信息，不写完成稿。
\`\`\`

### 小说/规范.md

\`\`\`
# 小说目录规范

在本目录写正文前，先读本文件、当前卷的 \`规范.md\`，以及当前三十章夹的 \`规范.md\`。

## 分卷

按卷分文件夹：\`卷一/\`、\`卷二/\`…。新开一卷时：创建该卷目录和 \`规范.md\`，更新本文件和根目录 \`目录结构.md\`。

## 每 30 章一个文件夹

每一卷内，按章号闭区间分夹，文件夹名用三位数字：

- \`001-030\`：第 1–30 章
- \`031-060\`：第 31–60 章
- \`061-090\`：第 61–90 章
- 以此类推

第 31、61、91… 章必须先建下一夹及其 \`规范.md\`，再写章文件。禁止把第 31 章放进 \`001-030\`。不要提前创建还用不到的空夹。

## 章文件

- 一章一个 Markdown。
- 文件名：\`第001章.md\`（序号三位）；确定标题后可以是 \`第001章-短标题.md\`。
- 正文只写该章故事。设定变更写回 \`设定/\`，结构问题记 \`修订/\`。

## 改目录

新建卷或三十章夹时，必须同时改：本文件、该卷 \`规范.md\`、根目录 \`目录结构.md\`。
\`\`\`

### 小说/卷一/规范.md

\`\`\`
# 卷一

本卷正文只放在本目录下的三十章夹中。当前应有 \`001-030/\`。写第 31 章前，在本目录创建 \`031-060/规范.md\`，并更新 \`../规范.md\` 与根目录 \`目录结构.md\`。本卷写完再开 \`小说/卷二/\`，不要在本卷继续堆下一卷的章。
\`\`\`

### 小说/卷一/001-030/规范.md

\`\`\`
# 第001–030章

本夹只放第 1 章到第 30 章，一章一个 \`第XXX章.md\`。写第 31 章前：在 \`小说/卷一/\` 下新建 \`031-060/\` 和它的 \`规范.md\`，并更新 \`小说/卷一/规范.md\`、\`小说/规范.md\`、根目录 \`目录结构.md\`。不要在本夹创建第 31 章。
\`\`\`

### 修订/规范.md

\`\`\`
# 修订规范

只记录问题、矛盾和待改项，不要直接改 \`小说/\` 里已定稿的位置来「顺便重写」。每条写清：位置（卷/章）、现象、建议。增删子文件夹时改本文件和 \`目录结构.md\`。
\`\`\`

### 修订/问题清单.md

一行说明「一条一行；改完再勾掉」，不要编问题。

## 5. 创建后

列出实际新建的路径（若建了写作根文件夹，把它一并列出），以及因已存在而跳过的路径。

然后明确告诉用户：

- 总约定在写作根目录的 \`目录结构.md\`；每个主要目录都有 \`规范.md\`。
- 之后写作，先进入写作根目录，再读目标目录的规范，然后改文件。
- 以后每写到第 31、61、91… 章，先建新的三十章夹并改规范，再写正文。
- 改了文件夹名称或层级，必须同时改 \`目录结构.md\` 和相关 \`规范.md\`。

不要在创建后直接开始编故事，除非用户接着要求。
`

export const WRITER_SKILL = {
  name: WRITER_SKILL_NAME,
  description: WRITER_SKILL_DESCRIPTION,
  content: WRITER_SKILL_CONTENT
} as const

export function isWorkbenchTemplateSkillName(name: string): boolean {
  return name === STORY_INIT_SKILL_NAME
}
